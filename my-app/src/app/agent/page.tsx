'use client';

//This page wraps the logistics agent interface for routing
import LogisticsAgent from '@/app/pages/logisticsAgent';

export default function AgentPage() {
  return <LogisticsAgent />;
}
